\documentclass[aip,jcp,preprint]{revtex4-1}

\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{braket}
\usepackage{booktabs}
\usepackage[capitalize]{cleveref}

\newcommand{\Tr}{\operatorname{Tr}}
\newcommand{\diag}{\operatorname{diag}}

\begin{document}

\title{Technical Notes: CASSCF Wavefunction Overlaps via Nonunitary
Biorthogonalization}
\author{Forte2 Developers}
\noaffiliation
\date{\today}
\maketitle

\section{The problem and the idea}

We want $\braket{\Psi_1|\Psi_2}$ for two CASSCF wavefunctions whose orbitals
were optimized independently: two states from separate optimizations, or the
same state at two geometries. The orbital sets are individually orthonormal
but not orthonormal to each other.

Done directly, this is a double sum over determinant pairs, each term a
L\"owdin determinant of mutual spin-orbital overlaps,\cite{Loewdin1955,Plasser2016}
\begin{equation}
 \braket{\Psi_1|\Psi_2}
 = \sum_{KL} c_1(K)^{*} c_2(L) \braket{\Phi_K|\Phi'_L},
 \qquad
 \braket{\Phi_K|\Phi'_L} = \det \mathbf{S}^{KL},
 \label{eq:naive}
\end{equation}
costing $O(n_{\det,1} n_{\det,2})$ determinant evaluations. Plasser
\emph{et al.}\cite{Plasser2016} attack this head-on, by never forming most of
those determinants twice.

Malmqvist\cite{Malmqvist1986} takes the other route, and it is the one
implemented here. If the two orbital sets can be transformed to a
\emph{biorthonormal} pair --- $\varphi^A = \varphi^X C^{XA}$,
$\varphi^B = \varphi^Y C^{YB}$ with $(C^{XA})^\dagger S^{XY} C^{YB} = 1$ ---
then every mutual overlap matrix in \cref{eq:naive} becomes an identity block,
each determinant overlap collapses to $\delta_{KL}$, and the whole double sum
becomes a single dot product of the two re-expressed CI vectors:
\begin{equation}
 \boxed{\;\braket{\Psi_1|\Psi_2} = c_1'^\dagger c_2'\;}
 \label{eq:payoff}
\end{equation}
(Malmqvist Eq.~(11), p.~492). The cost moves entirely into re-expressing each
CI vector in its new basis, which is a handful of sigma-vector builds.

The rest of this note is about making \cref{eq:payoff} concrete: how to build
the transformation (\cref{sec:biorth}), what it does to a CI vector
(\cref{sec:ci}), how it is applied in practice (\cref{sec:impl}), and the one
numerical pitfall that matters more than all the others put together
(\cref{sec:cores}).

Both regimes forte2 supports share the same machinery:
\begin{center}
\begin{tabular}{@{}p{0.20\linewidth}p{0.34\linewidth}p{0.34\linewidth}@{}}
\toprule
Regime & Orbitals & Inactive occupation \\
\midrule
Nonrelativistic & real, spin-restricted spatial MOs & doubly occupied \\
Two-component & complex spinors (GHF/X2C) & singly occupied \\
\bottomrule
\end{tabular}
\end{center}

\section{Notation}

$X$ and $Y$ are the two orbital sets, sharing a partition into $n_I$ inactive
(``docc'') and $n_T$ active orbitals, with no restricted/virtual block. $K,L$
index determinants; $I,T$ always label the inactive and active blocks. The
mixed MO overlap is
\begin{equation}
 S^{XY} = C_X^\dagger S_\mathrm{AO}(X,Y)\, C_Y
 = \begin{pmatrix} S_{II} & S_{IT} \\ S_{TI} & S_{TT} \end{pmatrix},
 \label{eq:mixed-overlap}
\end{equation}
with $S_\mathrm{AO}(X,Y)$ the AO overlap between the (possibly distinct)
geometries or basis sets behind $X$ and $Y$.

\section{Building the biorthonormal pair}
\label{sec:biorth}

Not every transformation is allowed. The CAS expansion is closed only under
the subgroup whose active-row/docc-column block vanishes (Malmqvist Sec.~V,
Fig.~1): new inactive orbitals must be pure recombinations of old inactive
orbitals, while new active orbitals may absorb inactive character. Both
$C^{XA}$ and $C^{YB}$ are built block upper-triangular for this reason.

\texttt{biorthogonalize\_casscf\_orbitals} follows Malmqvist's Appendix,
Eqs.~(A.1)--(A.7):

\begin{enumerate}
\item \textbf{(A.2)} SVD the inactive block, $S_{II}=U_{1I} D_I U_{2I}^\dagger$.
A vanishing $D_I$ means no biorthonormal pair exists within the CAS-closed
group.
\item \textbf{(A.3)} Remove inactive admixture from the active block,
$S'_{TT} = S_{TT} - S_{TI} U_{2I} D_I^{-1} U_{1I}^\dagger S_{IT}$.
\item \textbf{(A.4)} SVD it, $S'_{TT}=U_{1T} D_T U_{2T}^\dagger$.
\item \textbf{(A.5)} Rotate to the orthonormal ``pseudo-corresponding'' basis,
leaving residual inactive--active overlaps $S^{P_1P_2}_{IT}$, $S^{P_1P_2}_{TI}$
(not Hermitian conjugates: $P_1$ and $P_2$ are different orbital sets).
\item \textbf{(A.6)} Shift to the nonorthogonal basis, each side's active
orbitals absorbing enough of its own inactive orbitals to cancel those blocks.
\item \textbf{(A.7)} Rescale the $B$ side by $d=(D_I,D_T)$, giving
$(C^{XA})^\dagger S^{XY} C^{YB}=1$ exactly.
\end{enumerate}

Step (A.6) is worth pausing on: \textbf{it does not change the CI vector at
all.} Adding a multiple of an always-fully-occupied orbital to another
occupied orbital leaves a Slater determinant unchanged. Malmqvist states this
directly --- ``this final orbital transformation leaves the CI expansion
unaltered.'' So only steps (A.4) and (A.7) touch the CI coefficients, and the
active-space work reduces to \emph{one orthogonal rotation and one diagonal
rescale}. That fact drives the implementation in \cref{sec:impl}.

The SVDs are unique only up to rotations within degenerate singular subspaces,
so $C^{XA}$ and $C^{YB}$ can come back far from the identity even for nearly
identical orbital sets. This is a valid solution, not an error, and the
CI-vector transform is built to tolerate large rotation angles.

\section{What the transformation does to a CI vector}
\label{sec:ci}

For an invertible orbital transformation
$\varphi^\mathrm{new}=\varphi^\mathrm{old} M$, the CI-vector map is the
one-body Fock-space representation
\begin{equation}
 \rho[M] = \exp\!\Big(-\sum_{pq}(\log M)_{pq}\,E_{pq}\Big),
 \label{eq:rho-def}
\end{equation}
with $E_{pq}=p_\alpha^\dagger q_\alpha+p_\beta^\dagger q_\beta$
nonrelativistically, and $E_{pq}=p^\dagger q$ for spinors
(\cref{sec:twocomp}). Because orbitals transform by \emph{right}
multiplication, composition reverses on the CI side:
\begin{equation}
 \rho[M_1M_2] = \rho[M_2]\,\rho[M_1].
 \label{eq:anti}
\end{equation}
This anti-homomorphism is easy to get backwards, and doing so raises no error
--- during development it silently returned $0.908$ where $1.0$ was correct.
Any new factorization must have its order derived from \cref{eq:anti}, not
guessed.

\subsection{The inactive block is a scalar}

$M$ is block upper-triangular, and for such a matrix any analytic function ---
$\log$ included --- is block upper-triangular with the same zero block and
with $(\log M)_{II} = \log M_{II}$. Two consequences, given that every
determinant in a CAS expansion has the inactive block completely full:

\begin{itemize}
\item The zero $(T,I)$ block means the generator never promotes an electron
out of the inactive space, so the transformed wavefunction stays inside the
same CAS manifold. This is exactly the closure property of Malmqvist's
Sec.~V subgroup.
\item Every off-diagonal one-body term with an inactive creation index is
Pauli-blocked: the target inactive orbital is already full. Only the diagonal
number operators survive.
\end{itemize}

So the entire inactive block contributes one state-independent scalar,
\begin{equation}
 \rho[M]\big|_{I}
 = \exp\!\Big(-\sum_{p\in I} t_{pp}\, n_p\Big)
 = \det(M_{II})^{-n_\mathrm{ch}},
 \label{eq:docc-scalar}
\end{equation}
where $n_\mathrm{ch}$ counts occupied second-quantized channels per inactive
orbital: $n_\mathrm{ch}=2$ nonrelativistically (a doubly occupied spatial
orbital contributes $\det(M_{II})^{-1}$ per spin), and $n_\mathrm{ch}=1$ for
singly occupied spinors. This reproduces Malmqvist's factor
$\prod_i (t_{ii})^2$ (p.~489) and the explicit factor of 2 in his Eq.~(10),
p.~492.

Only the active--active block needs a genuine generator.

\section{Implementation}
\label{sec:impl}

\subsection{Use the factors, not their product}

The active block of $C^{XA}$ is \emph{exactly} the orthogonal factor $U_{1T}$,
and that of $C^{YB}$ is \emph{exactly} $U_{2T}D_T^{-1}$. Rather than
multiplying these together and taking a matrix logarithm of the product, the
implementation keeps them --- \texttt{biorthogonalize\_casscf\_orbitals}
returns a \texttt{BiorthogonalOrbitals} carrying \texttt{U\_actv\_A},
\texttt{U\_actv\_B} and \texttt{d\_actv} alongside the assembled matrices ---
and applies them as two exact steps. Per \cref{eq:anti}, the rotation is
applied to the CI vector first and the rescale second:
\begin{equation}
 \rho[U_{2T}D_T^{-1}] = \rho[D_T^{-1}]\,\rho[U_{2T}].
\end{equation}

This matters for two reasons. First, the diagonal rescale then has a closed
form --- Malmqvist's Appendix step 6 --- costing one pass over the determinant
list:
\begin{equation}
 \rho[D_T^{-1}]\,c(\mu) = c(\mu) \prod_t d_t^{\,n_t^\mu},
 \label{eq:diag-closed}
\end{equation}
with $n_t^\mu$ the occupation of active orbital $t$ in determinant $\mu$.
Routing it through \cref{eq:rho-def} instead would demand a Taylor series
whose length grows with the spread of $d_T$ --- the worst-conditioned piece of
the whole transformation, and entirely avoidable. Second, the only matrix
logarithm left is that of an orthogonal matrix, whose norm is bounded by
$\pi$.

\subsection{Improper rotations}

$U_{1T}$ and $U_{2T}$ are orthogonal but may be \emph{improper}
($\det = -1$), which has no real logarithm at all, since
$\det(\exp\kappa)=\exp(\Tr\kappa)>0$ for real $\kappa$. Such a factor is split
as $U = FR$ with $F$ flipping the sign of active orbital 0 and $R$ proper.
$F$'s CI action is exact and closed-form --- multiply each coefficient by
$(-1)^{n_0}$ --- and $R$ is logged from its real Schur form, reading each
$2\times2$ block's angle with \texttt{arctan2}. That is branch-free over the
full range, unlike \texttt{scipy.linalg.logm}, whose principal branch returns
a spuriously complex result for rotations near $\pi$. By \cref{eq:anti} the
reflection is applied before $R$'s generator.

\subsection{Applying the generator}

\texttt{transform\_ci\_vector\_direct} evaluates $\exp(-T)c$ by
scaling-and-squaring: halve the generator $m$ times until its spectral norm
falls below a threshold, Taylor-expand that well-conditioned exponential, and
apply the result $2^m$ times, which is exact since
$\exp(-T)=\big(\exp(-T/2^m)\big)^{2^m}$. Each application is a one-electron
sigma build (\texttt{CISigmaBuilder.sigma\_one\_electron} with $V=0$), the
same machinery every CI run already uses. Note that this is not Malmqvist's
own recommendation: his Sec.~IV gives an exact finite algorithm via an LU
partitioning and a sequence of single-orbital transformations, costing
$O(n_T)$ operations with no logarithm anywhere. The exponential route is
chosen here for simplicity, and is a reasonable trade only because
\cref{eq:diag-closed} removes the ill-conditioned part.

\subsection{Two-component specialization}
\label{sec:twocomp}

Spinors carry both spin components internally, so there is no $\alpha/\beta$
pairing; \texttt{RelCISigmaBuilder} packs all active electrons into a single
string. Four things change:

\begin{enumerate}
\item \textbf{Single-channel generator}, $E_{pq}=p^\dagger q$.
\item \textbf{$n_\mathrm{ch}=1$} in \cref{eq:docc-scalar}, since inactive
spinors are singly occupied. Getting this wrong returns a plausible but wrong
number rather than failing, so it is pinned by a dedicated test
(\cref{sec:verify}).
\item \textbf{No branch-cut problem.} $\exp\colon\mathfrak{gl}(n,\mathbb C)
\to GL(n,\mathbb C)$ is surjective, so every invertible complex matrix has a
logarithm; \texttt{logm} is applied directly with no reflection handling. (The
real obstruction above is visible in $\det(\exp\kappa)>0$, but that condition
alone is necessary, not sufficient --- surjectivity follows from the Jordan
form, every eigenvalue being nonzero and admitting a complex logarithm.)
Verified numerically: even for a matrix with an eigenvalue exactly at $-1$,
$\exp(\log U)=U$ holds to $2\times10^{-15}$.
\item \textbf{Spin-doubled AO overlap.} Forte2 stores spinor coefficients as
$\binom{C^\alpha}{C^\beta}$, so \cref{eq:mixed-overlap} needs
$S_\mathrm{AO}\to\diag(S_\mathrm{AO},S_\mathrm{AO})$. \texttt{mo\_overlap}
applies \texttt{block\_diag\_2x2} whenever the system is two-component,
matching \texttt{System.ints\_overlap()}.
\end{enumerate}

\texttt{casscf\_wavefunction\_overlap} reads this off
\texttt{system\_1.two\_component} and rejects a mismatched pair, rather than
taking a flag that could disagree with the orbitals passed in.

\section{Core orbitals: the dominant numerical pitfall}
\label{sec:cores}

For overlaps between different geometries, the largest error source is not the
algorithm --- it is the core orbitals. Tight cores follow their nuclei, so
their mutual overlap decays steeply with displacement, and that decay
multiplies straight into \cref{eq:payoff}. Plasser \emph{et al.}\ document this
in their Sec.~3.3.

Rigidly translating a molecule leaves its wavefunction physically unchanged,
so the overlap ought to be 1. Measured with this implementation (STO-3G,
overlap of an identical state against its own translate):

\begin{center}
\begin{tabular}{@{}lcccc@{}}
\toprule
& $n_\mathrm{core}$ & 0.05 bohr & 0.1 bohr & 0.2 bohr \\
\midrule
LiH & 1 & 0.9935 & 0.9744 & 0.9030 \\
FH  & 4 & 0.9288 & 0.7532 & 0.3585 \\
ClH & 8 & 0.7407 & 0.3342 & 0.0256 \\
\bottomrule
\end{tabular}
\end{center}

The artifact grows sharply with nuclear charge --- for HCl a displacement of
0.1~\AA{} destroys the overlap entirely. This is worst exactly where the
two-component path is used, since X2C is reached for precisely the heavy
elements with the tightest cores.

The mitigation, following Plasser's Eq.~(21), is to discard core orbitals from
the overlap, assuming they are orthonormal between the two states and
orthogonal to everything retained. \texttt{casscf\_wavefunction\_overlap}
exposes this as \texttt{n\_frozen\_docc}. For FH displaced by 0.1 bohr,
freezing the 1s lifts the overlap from 0.75 to 0.94.

\textbf{Freeze only tight cores.} The assumption fails for valence orbitals,
and over-freezing is worse than not freezing at all: for the same FH case,
freezing all four inactive orbitals gives 0.05. The two halves of the
assumption behave very differently, and only one is diagnostic:

\begin{center}
\begin{tabular}{@{}p{0.42\linewidth}p{0.50\linewidth}@{}}
\toprule
Assumption & Behavior \\
\midrule
Frozen orbitals mutually orthonormal & Fails by design whenever nuclei move;
removing this decay is the \emph{point} of freezing. Not checked. \\
Frozen orthogonal to retained orbitals & Fails only when freezing is genuinely
invalid. Checked, with a warning. \\
\bottomrule
\end{tabular}
\end{center}

For FH at 0.1 bohr the frozen--retained coupling is $0.09$ when freezing the 1s
(fine) and $0.97$ when freezing all four (invalid), so the check separates the
two cases cleanly.

\section{Code map}

\begin{center}
\begin{tabular}{@{}p{0.42\linewidth}p{0.50\linewidth}@{}}
\toprule
File or function & Responsibility \\
\midrule
\texttt{orbitals/wavefunction\_overlap.py} & Biorthogonalization, CI-vector
transforms, both backends, dispatcher, validation. \\
\texttt{orbitals/orbital\_overlap.py} & \texttt{mo\_overlap}, including
two-component AO-overlap doubling. \\
\texttt{ci/ci\_sigma\_builder.\{h,cc\}} & \texttt{sigma\_one\_electron},
\texttt{sigma\_two\_electron}, \texttt{set\_Hamiltonian}. \\
\texttt{ci/rel\_ci\_sigma\_builder.\{h,cc\}} & The same, complex128. \\
\texttt{tests/orbitals/test\_wavefunction\_overlap.py} & End-to-end tests,
both regimes. \\
\bottomrule
\end{tabular}
\end{center}

The C++ primitives are thin wrappers splitting the existing sigma
contributions; neither assumes a symmetric $H$, which matters because
$t=\log M$ is a rotation generator, not a Hamiltonian.

\section{Verification}
\label{sec:verify}

\begin{center}
\begin{tabular}{@{}p{0.34\linewidth}p{0.58\linewidth}@{}}
\toprule
Test & What it pins down \\
\midrule
Biorthogonalization algebra & $(C^{XA})^\dagger S C^{YB}=1$ to $10^{-10}$,
block triangularity, and that the returned factors reconstruct the assembled
blocks exactly. \\
Self-overlap & Returns 1 for a wavefunction against itself, both regimes. \\
Rotated active orbitals & Recovers 1 against a reference CI vector built
independently with \texttt{SparseExp}, both regimes. \\
Two independent CASSCF runs & Cross-checked against a from-scratch
$O(n_{\det}^2)$ L\"owdin sum (\cref{eq:naive}) sharing no code with the
module. \\
Backend cross-check & \texttt{direct} vs \texttt{sparse\_ops}, including a
large-angle case; \texttt{sparse\_ops} deliberately still uses the combined
matrix and a single \texttt{logm}, so it independently validates the factored
route. \\
Two-component docc power & A pure inactive-spinor rotation must return exactly
$\det(U_\mathrm{docc})^{n_\mathrm{ch}}$; $n_\mathrm{ch}=2$ misses by
$\mathcal{O}(1)$, so the test is sharply discriminating. \\
Frozen core & The translated-FH artifact is present, and freezing the 1s
removes most of it. \\
Frozen-core guard & Warns when freezing valence orbitals, silent when freezing
only the 1s. \\
Input validation & Two-component mismatch, bad \texttt{n\_frozen\_docc},
wrong CI-vector length, wrong orbital-matrix shape. \\
\bottomrule
\end{tabular}
\end{center}

A physical check outside the automated suite: scanning
$\braket{\Psi(R_e)|\Psi(R)}$ for LiH out to 6 bohr reproduces a smooth decay,
a sign flip, and partial recovery near $R=5$ bohr --- the signature of LiH's
ionic--covalent avoided crossing. The two backends agree to six decimals,
differing in overall sign only very near the crossing, where the phase is
inherently ambiguous.

\section{Limitations}

\begin{itemize}
\item Only a single CAS block; general (GAS) restrictions are not handled,
though Malmqvist's Sec.~V covers them in principle.
\item Single pairs only. Plasser's overlap-matrix orthogonalization
(his Eqs.~(25)--(26)), which repairs displacement artifacts far better than
core freezing alone and diagnoses missing states via deviation from
orthogonality, needs an $N\times N$ matrix of state pairs and is not
implemented.
\item No CI-vector truncation. Plasser's Sec.~3.2 threshold, and the
renormalization of his Eq.~(20), would matter for expansions much larger than
CASSCF.
\item The exponential route costs a few times more sigma builds than
Malmqvist's exact LU algorithm; see \cref{sec:impl}.
\item Comparing a two-component wavefunction against a nonrelativistic one is
rejected, not supported.
\end{itemize}

\begin{thebibliography}{9}
\bibitem{Loewdin1955}
P.-O. L\"owdin, Phys. Rev. \textbf{97}, 1474 (1955).
\bibitem{Malmqvist1986}
P.-\AA.~Malmqvist,
``Calculation of transition density matrices by nonunitary orbital
transformations,''
Int. J. Quantum Chem. \textbf{30}, 479--494 (1986).
\bibitem{Plasser2016}
F. Plasser, M. Ruckenbauer, S. Mai, M. Oppel, P. Marquetand, and
L. Gonz\'alez,
``Efficient and flexible computation of many-electron wave function
overlaps,''
J. Chem. Theory Comput. \textbf{12}, 1207--1219 (2016).
\end{thebibliography}

\end{document}
